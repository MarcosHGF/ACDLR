# Tests

Small regression tests for the reproducibility artifact.

Run:

```powershell
python -m pytest
```

The tests focus on deterministic pieces of the repository, especially the
evaluation metrics used by the ACDLR x CNN comparison.
