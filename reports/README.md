# Reports

Human-readable, stable reports can be placed here when preparing a paper or
presentation. Generated experiment outputs are written to `artifacts/` and are
ignored by Git.

Current generated report:

```text
artifacts/acdlr_vs_crater_cnn/comparison_report.md
```

For a submission package, copy selected stable tables/figures from `artifacts/`
into this folder and cite the command that generated them.
